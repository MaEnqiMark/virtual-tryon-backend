fastapi
uvicorn[standard]

pandas
numpy

# For file uploads (UploadFile / multipart form data)
python-multipart

# For HTTP download of the dataset zip
requests

# OpenAI + image handling (very likely already in your project)
openai
Pillow
